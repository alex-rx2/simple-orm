package simple.orm.jdbc.map.in;

/**
 * TODO
 * - map params one by one (as separate objects)
 * - map params from object fields (getters, direct field access)
 * - map by index, map by query param name
 * <p>
 * structure source prepare(once) -> extract -> validate -> inject
 */
public interface InMapper {

    // TODO
}
